# Virtual Machines outlive the Agent

If the Agent on a Device stops, running Virtual Machines on that Device keep running. When the Agent starts again, it reconnects to guests that are still there. Stopping BarkVisor is not stopping the guests; the person stops Workloads in the console first.
