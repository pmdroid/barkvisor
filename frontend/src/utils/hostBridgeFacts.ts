/** Defaults matching HostBridgeFactsService (PAS-236). Device probe overrides these. */
export const HOST_BRIDGE_SUGGESTED = 'br0'
export const HOST_BRIDGE_HELPER_PATH = '/usr/lib/qemu/qemu-bridge-helper'
export const HOST_BRIDGE_ACL_PATH = '/etc/qemu/bridge.conf'
